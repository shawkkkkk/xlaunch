# Privy's optional permissionless peer currently pins an older ox range than
# XLaunch's viem dependency. XLaunch does not use Privy smart accounts /
# permissionless, so do not fail installs on that optional peer mismatch.
legacy-peer-deps=true
